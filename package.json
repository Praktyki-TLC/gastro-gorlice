{
  "name": "gastro-gorlice",
  "version": "1.0.0",
  "description": "Monorepo projektu gastro-gorlice.pl",
  "scripts": {
    "dev": "pnpm recursive run dev",
    "build": "pnpm recursive run build",
    "lint": "pnpm recursive run lint",
    "test": "pnpm recursive run test",
    "db:generate": "pnpm --filter=backend drizzle-kit generate",
    "db:push": "pnpm --filter=backend drizzle-kit push",
    "backend:deploy": "pnpm --filter=backend deploy",
    "frontend:deploy": "pnpm --filter=frontend deploy"
  },
  "keywords": [],
  "author": "Praktyki TLC",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "eslint": "^10.0.3",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  },
  "repository": {
    "url": "https://github.com/Praktyki-TLC/gastro-gorlice"
  },
  "private": true
}
